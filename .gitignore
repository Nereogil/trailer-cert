# Never commit real job data or credentials
*.xlsx
!samples/*.template.xlsx
*.pdf
secrets.json
.env
node_modules/
.DS_Store
Thumbs.db

# Real job data, photos and certificates stay local
samples/

# API keys belong in the app, never in the repo
api.txt
*.key
*api*key*
